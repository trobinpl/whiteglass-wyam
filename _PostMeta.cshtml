@{
    var Published = Model.Get<DateTime>(BlogKeys.Published);
    var Author = Model.Metadata.Get<string>("Author");
}
@if(string.IsNullOrEmpty(Author))
{
    <time datetime=$"@Published.ToString("o")" itemprop="datePublished">@Published.ToString("dd MMMM yyyy") r.</time> @Html.Partial("_TagsLinks", Model)
}
else{
    @Author @Html.Raw("•") <time datetime=$"@Published.ToString("o")" itemprop="datePublished">@Published.ToString("dd MMMM yyyy") r.</time> @Html.Partial("_TagsLinks", Model)
}
